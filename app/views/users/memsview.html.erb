<% provide(:title, 'Admin Console') %>
<h1>Member Details</h1>
<%= form_for (@user),:url=> url_for(:controller => 'users',:action =>'memsview') do |f| %>
    <p id="notice"><%= notice %></p>
    <p>
    <strong>Name:</strong>
    <%= @memsall.inspect %>
    </p>
        <!% mems = ActiveSupport::JSON.decode(@memsall) %>
        <!% mems["username"] %>
        <!% mems["email"] %>
        <!% @mems = User.where("username = ?",@memsall.username) %>
        <!% building_entry = @memsall.as_json[0]["mems"] %>
        <!%= "Username : #{building_entry}" %>
        <!% @memsn = User.where("email = ?",@memsall.email) %>
        <!% mems_entry = @memsall.as_json[0]["memsn"] %>
        <!%= "Email : #{mems_entry}" %>
    <%= link_to 'Back to Admin Console', dumget_path%>
<%end%>